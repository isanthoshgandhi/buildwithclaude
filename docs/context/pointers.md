# Build with Claude — Session Pointers

> Read this file FIRST on every session.
> Pick your domain. Load ONLY those files. Never load everything.

## QUICK RESUME
Last session: 2026-03-22 (approx)
Where we left off: v1.2.1+ — 117 agents, 175 commands, 28 hooks, 26 skills, 50 plugins. Web UI live at buildwithclaude.com.
Next action: Check UPDATES.md for latest changes, then load domain file.

---

## Domain: content
*Agents, commands, hooks, skills, plugins — adding and maintaining plugin collections*

Load:
1. `docs/context/content.md` — what exists, counts, structure, next additions

Skip: platform.md, web-ui/, scripts/

---

## Domain: platform
*Web UI, marketplace discovery, sync scripts, Docker, MCP servers*

Load:
1. `docs/context/platform.md` — web UI status, sync workflow, infra

Skip: content.md, plugins/ directory

---

## What NOT to load
| File | Why skip |
|---|---|
| `UPDATES.md` | Full changelog — skim only for latest entry |
| `plugins/` | Source content — load only specific plugin when editing |
| `web-ui/` | Frontend code — load only when working on UI |
| `mcp-servers.json` | Data file — load only when updating MCP list |
